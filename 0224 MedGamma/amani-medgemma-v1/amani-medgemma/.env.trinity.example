# AMANI Trinity-Audit API Configuration
# Copy this file to .env and fill in your API keys

# ============================================
# Trinity-Audit Real API Mode
# ============================================
# Set to "true" to enable real API calls (GPT-4o + Claude + MedGemma)
# Set to "false" to use mock mode (default, no API keys needed)
AMANI_TRINITY_REAL_API=false

# ============================================
# OpenAI API Configuration (for GPT-4o)
# ============================================
# Get your API key from: https://platform.openai.com/api-keys
# Required for Trinity-Audit real mode
OPENAI_API_KEY=

# Example (DO NOT commit real keys):
# OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# Anthropic API Configuration (for Claude)
# ============================================
# Get your API key from: https://console.anthropic.com/settings/keys
# Required for Trinity-Audit real mode
ANTHROPIC_API_KEY=

# Example (DO NOT commit real keys):
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# MedGemma Configuration
# ============================================
# MedGemma runs locally (GPU/CPU/mock mode)
# No API key required
# Control via AMANI_MOCK_MODE (see main .env file)

# ============================================
# Usage Instructions
# ============================================
#
# 1. Mock Mode (default, no setup needed):
#    python -m l2_orchestrator.trinity_audit
#
# 2. Real API Mode:
#    a. Copy this file: cp .env.trinity.example .env
#    b. Fill in OPENAI_API_KEY and ANTHROPIC_API_KEY above
#    c. Set AMANI_TRINITY_REAL_API=true
#    d. Load environment: source .env (Linux/Mac) or set in Windows
#    e. Run: python -m l2_orchestrator.trinity_audit
#
# 3. In Python code:
#    from l2_orchestrator.trinity_audit import trinity_audit_real_api
#    result = trinity_audit_real_api(query, context)
#
# ============================================
# Security Notes
# ============================================
# - NEVER commit .env file to git
# - Add .env to .gitignore
# - Rotate API keys regularly
# - Use separate keys for dev/prod
# - Monitor API usage and costs
# - For production: use secret management service (AWS Secrets Manager, Azure Key Vault, etc.)

# ============================================
# Cost Estimates (as of Feb 2026)
# ============================================
# GPT-4o:
#   Input: $2.50 per 1M tokens
#   Output: $10.00 per 1M tokens
#   Avg Trinity call: ~500 input + 200 output = $0.003 per call
#
# Claude 3.5 Sonnet:
#   Input: $3.00 per 1M tokens
#   Output: $15.00 per 1M tokens
#   Avg Trinity call: ~500 input + 200 output = $0.0045 per call
#
# MedGemma 1.5 4B:
#   Local inference: FREE (GPU/CPU cost only)
#
# Total per Trinity consensus: ~$0.0075 (0.75 cents)
# 100 queries/day = $0.75/day = $22.50/month
