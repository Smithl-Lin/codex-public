# A.M.A.N.I. V4.0 — 环境变量模板
# 复制为 .env 后填入实际值，勿提交 .env 到版本库

# API Keys（从 config 模块读取，无默认值以防泄露）
GEMINI_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Google 服务账号 JSON 路径（Vertex / Gemini 等）
GOOGLE_APPLICATION_CREDENTIALS=

# MedGemma / Medical Reasoner（Phase 4）
MEDGEMMA_ENDPOINT=
MEDGEMMA_FINETUNE_VERSION=
